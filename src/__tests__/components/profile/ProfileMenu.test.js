import { profileMenu } from '../../../components/profile/ProfileMenu';

describe('Testing ProfileMenu', () => {
  it('Testing ProfileMenu components', () => {
    expect(profileMenu()).toBeTruthy();
  });
});
