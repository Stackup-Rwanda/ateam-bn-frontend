import { ProfileDetail } from '../../../components/profile/ProfileDetails';

describe('Testing ProfileDetails', () => {
  it('Testing ProfileDetails component', () => {
    expect(ProfileDetail()).toBeTruthy();
  });
});
